[package]
name = "credit_assignment"
version = "0.1.0"
edition = "2024"

[dependencies]
candle-vllm = { path = "../candle-vllm", features = [] }
tokio = {version="1.50.0", features=["full"]}
[features]
cuda=["candle-vllm/cuda"]