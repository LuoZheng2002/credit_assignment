use crate::direct_tool::{
    direct_tree_action_log::DirectTreeActionLog,
    posterior_schema::{PosteriorCalculationConfig, PosteriorFitPerTree},
};

pub fn action_log_to_posterior_fit(
    action_log: &DirectTreeActionLog,
    posterior_calculation_config: &PosteriorCalculationConfig,
) -> PosteriorFitPerTree {
    todo!()
}
